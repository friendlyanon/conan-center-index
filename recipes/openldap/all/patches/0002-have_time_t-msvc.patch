--- include/ac/time.h
+++ include/ac/time.h
@@ -25,7 +25,7 @@
 #endif
 # include <time.h>
 
-#if defined(_WIN32) && !defined(HAVE_CLOCK_GETTIME)
+#if defined(_WIN32) && !defined(HAVE_CLOCK_GETTIME) && !defined(HAVE_TIME_T)
 	struct timespec {
 		time_t	tv_sec;
 		int 	tv_nsec;
